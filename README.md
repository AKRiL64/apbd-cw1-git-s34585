For task 5: fast-forward merge couldn't be done for merging main and feature_max because this exact commit, where I add this exact text, changing the main branch, so git has to combine two diverging histories
For task 6: this is just a text so something changes in main before rebase
For task 7: fast-forward merge couldn't be done for merging main and change_modifiers_here because they had a conflict in added getter function (GetItem in class Inventory)

Q:

1. When the target branch has no new commits since the additional one was created
2. When two branches independendly modified same parts of code
3. I kept adding public modifier for constructor and "items" field in class Inventory from 'update_modifiers_here' branch, as well as kept version of GetItem from 'main' (the one, that can raise exeption)
